# CONTEXT — なおったメモ

このプロジェクトの用語集(ユビキタス言語)。実装の詳細は書かない。定義と語の区別だけを扱う。

## 中核の用語

### 症状 (Issue)
ユーザーが追跡する、ひとつの体調上の問題を表す**継続的なスレッド**。自由入力の名前を持つ(例:「頭痛」「右ひざの痛み」)。`active`(続いている)と `resolved`(治った)の間を**何度でも行き来できる**(治って、また再発して、また治って…)。1つの症状は生涯で複数回の発症〜治癒を含みうる。→ ADR 0001。
- **混同注意**: 「症状」は"1日の記録"ではない。1日の記録は[チェックイン]。
- **混同注意**: 「症状」は"1回の発症〜治癒の山"でもない。その山は[エピソード]。1つの症状は複数のエピソードを持ちうる。

### エピソード (Episode)
1つの[症状]における、1回の「発症 → 治癒」の山。`start`(または `relapse`)で始まり `resolved` で終わる。**独立した実体としては保存せず**、[チェックイン]の節目イベントの列から**導出する**ビュー(決定・ADR 0001)。気圧との突き合わせ(§6.4)や通院用の「今月◯回」表示は、このエピソード単位で行う。

### チェックイン (Checkin / Entry)
ある[症状]のタイムライン上の、**時刻つきの1エントリ**。**1日に何件でも置ける**(プロトタイプ§7の「1日1件」制約は撤廃・ADR 0003)。「状態」と「メモ」を任意で持つ(Phase 2 で数値も。→ [[measurement]])。タップするたびに新しいエントリが**足される**(上書きしない)。「今日の状態」は、その日の**最新の状態エントリ**で表す。start/resolved/relapse も worse/same/better も、すべてこの同じ時刻つきエントリの流れの中のイベントであり、[エピソード]はこの流れから導出する。

- **状態(Status)**: 6値で確定。2軸に分かれる。→ ADR 0001
  - **経過(強さの前回比)**: `worse`(悪化)/ `same`(変わらず)/ `better`(改善)。日々のボタンはこの3つ。
  - **ライフサイクルの節目**: `start`(発症)/ `resolved`(治った)/ `relapse`(再発)。登録・治癒・再発の操作から付く。
  - **中立のメモ**: `memo`(メモ)。強さの判断(worse/same/better)を選ばずにメモだけ残すための状態(例「病院に行った」)。active/resolved は変えず、現在のエピソードに属する。→ ADR 0006。
    - **経緯**: 一度は "other"(その他)を廃止したが、実利用で「状態を選ばずメモだけ確定したい」導線が無く不便と判明。用途を明確化した `memo` として復活させた(ADR 0006)。
- **メモ(note)**: **どの状態にも付けられる任意の自由記述**(決定)。`other` の付属品ではない。症状の"質"の変化(例「ズキンズキン→二日酔い型」)も、状態ですらない出来事(例「病院に行った」)も、ここに書ける。空でもよい。

**記録の訂正**: 押し間違い・日付違い・状態違いは必ず起きる前提で、**どのエントリ(経過も節目も)も後から編集・削除できる**(決定)。記録は上書きせず足していくので、朝の記録が夜の記録で静かに消えることはない。エピソード([エピソード])はイベント列から導出するので、訂正すれば山の区切りは自動で再計算される。「[治癒]の取り消し」は専用ボタンではなく、この汎用訂正で治癒イベントを削除することで行う。一方[再発]は"前向きの新規操作"として独立させる。

**日内の観察**: 発熱の体温を1日に何度も測る、尿の色が変わる、といった日内変化は、時刻つきエントリを足していくことで記録する(決定)。数値(体温℃)も定性(尿が濃い)も同じ器。数値の構造化・グラフ化・自動集計(最高体温など)は Phase 2 の[計測]で扱う。

### 治癒 (Resolved)
[症状]が「治った」状態へ遷移すること、およびその状態。治った日(resolvedDate)を伴う。このアプリの存在理由は、この治癒の記録を"取りこぼさない"こと。
- **混同注意**: 「改善(better)」は1日の[チェックイン]の状態にすぎず、治癒とは別。治癒は `better` の記録とは**独立**していて、`active` な症状ならいつでも確定できる(決定)。治った日(resolvedDate)は"気づいた日"に固定せず、実際に治った日を選べる(既定=今日、「昨日/日付を選ぶ」も可)。「思い出して開いた日」と「治った日」を分離するため。

### 現在エピソードの経過日数
[症状]がいま続いている日数。直近の `start` または `relapse` から今日まで(治癒中なら数えない)。カードの主役の数字。「このぶり返し、もう4日目」を表す。→ [[time-concepts]]

### 罹病スパン (Disease span)
[症状]が最初に発症した日(`Issue.startDate` = 原初の発症、固定)から現在(active なら今日 / resolved なら最後の治癒日)までの通算期間。「2年」「32歳から」など、慢性・体質性を表す。エピソードが複数あり間にすき間があれば「**断続的**」、ひと続きなら「継続的」とアプリが自動で判定して表現に添える。通院用の要約文(例「32歳から断続的に蕁麻疹」)はここから生成する。「◯歳から」の表現には**任意設定の生年月日(端末内のみ)**を使う(決定)。発症日は正確な日付で出るため、生年月日から**発症時点の正確な年齢**を出す(「32歳(2024年3月3日)から」)。未設定なら日付だけで表現する。
- **混同注意**: [現在エピソードの経過日数]とは別物。片頭痛が「2年間・断続的(罹病スパン)」でも「今のぶり返しは4日目(現在エピソード経過日数)」ということが両立する。

### 再発 (Relapse)
一度[治癒]した[症状]が、ぶり返して再び `active` になること。臨床的に意味のある転換点であり、単なる誤操作の取り消し(undo)とは別物。再発した日を伴う。
- **紐付けは本人が選ぶ(決定)**: 「この腰痛が前と同じ種類か」は文字列一致では判定できず、本人にしか分からない。よって再発は、**治癒済みの症状を新しい順に列挙し、本人が該当スレッドを選ぶ**ことで繋ぐ。名前の機械的一致には頼らない。
- **「新規登録」と「再発」は別の入口(決定)**: 名前を打てば新しいスレッド(新規登録)。治癒済みから選べば既存スレッドの再発。どちらかは本人が決める。この設計により、症状名の表記ゆれ(頭痛/頭が痛い)がスレッドを割る問題は"紐付け"に関しては解消する。
- **混同注意**: 「誤って治癒にしてしまったので戻す」操作(undo)と、「本当にぶり返した」再発は、意味が違う。undo は治癒イベントの削除(汎用訂正)、再発は前向きの新規操作。→ ADR 0001

### 計測 (Measurement)
症状に紐づく、単位つきの数値記録(例:体温 39.1℃)。時刻つきの[チェックイン]の流れの中に、数値として乗る。**構造化・グラフ化・自動集計(最高体温など)を伴うのは Phase 2**(決定・ADR 0003)。Phase 1 では数値も定性観察も区別なく時刻つきエントリのメモとして書ける。将来 体重・血圧も同じ器を再利用する想定。

### デイリームード / 今日の調子 (Daily Mood)
特定の[症状]に紐づかない、その日の総合的な体調。4段階(◎絶好調 / ○普通 / △だるい / ✕つらい)で1日1件。症状のない「良い日」も記録に残すためのもの。
- **混同注意**: 姉妹アプリ「アサトモ目覚まし」が毎朝送る「元気」とは**別概念として並立**させる(決定)。「元気」= 起床の瞬間の気分を見守り相手へ送る社会的シグナル。「調子(デイリームード)」= その日全体の体調を自分の記録として残すもの。時間軸(起床時 / 1日全体)も用途(対他者 / 対自分)も異なる。値の受け渡し統合はしない。

## 関連コンテキスト

- **アサトモ目覚まし** (`../asatomo/android`): 同じ作者の Android アラームアプリ。起床時の「起きました」操作を主要な再訪導線として、このアプリへディープリンクする案がある。
